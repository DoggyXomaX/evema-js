Evema.Modules[ "Schema" ] = ( function() {
const Schema = {}

Schema.Init = function() {

};

Schema.Actions = {
  'Init' : Schema.Init
};

Schema.Options = {
  Standard: {
    
  },
  Current: {}
};

return Schema; } )();
